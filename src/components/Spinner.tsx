const Spinner = () => {
  return (
    <div className="h-full flex justify-center items-center">
      <div className="w-24 h-24 rounded-[50%] bg-spinner animate-spin" />
    </div>
  );
};

export default Spinner;
